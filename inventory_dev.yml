---
all:
  children:
    dev:
      hosts:
        127.0.0.1: # Due to the way this template is configured, DO NOT CHANGE THIS!
      vars:
        connection: local
    automationcontroller:
      hosts:
        https://aap-controller-aap.apps.cluster-2tbwr-1.dynamic.redhatworkshops.io

    automationhub:
      hosts:
        https://aap-hub-aap.apps.cluster-2tbwr-1.dynamic.redhatworkshops.io/

    automationedacontroller:
      hosts:
        https://aap-eda-aap.apps.cluster-2tbwr-1.dynamic.redhatworkshops.io

    #builder:
     # hosts:
      #  HERE: # FQDN of builder host if you have one. Otherwise, can be Automation Hub host or commented out if not in use.

  vars:
    env: dev
...
